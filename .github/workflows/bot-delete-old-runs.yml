name: bot-delete-old-runs

on:
  schedule:
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch: null

jobs:
  delete-old-runs:
    name: delete-old-runs
    runs-on: "ubuntu-latest"
    steps:
      - name: get env file
        shell: bash -l {0}
        # TODO: Pull this environment.yml from the archived repodata-tools into cf-scripts
        run: |
          wget https://raw.githubusercontent.com/conda-forge/repodata-tools/main/environment.yml

      - name: install micromamba
        uses: mamba-org/provision-with-micromamba@main

      # TODO: Move this python script into its own file
      - name: delete runs
        shell: bash -l {0}
        run: |
          echo "\
          import github
          import os
          import sys
          import requests
          import datetime
          import time

          gh = github.Github(os.environ['GITHUB_TOKEN'])
          r = gh.get_repo('regro/cf-scripts')
          done = 0
          for w in r.get_workflows():
              for _ in range(10):
                  for rn in w.get_runs().reversed:
                      if rn.status == 'completed' and (
                          datetime.datetime.utcnow() - rn.updated_at > datetime.timedelta(days=90)
                      ):
                          requests.delete(
                              rn.url,
                              headers={'Authorization': 'Bearer ' + os.environ['GITHUB_TOKEN']},
                          )
                          done += 1
                          time.sleep(1)
                      if done == 500:
                          sys.exit(0)
          " > del_old_runs.py

          python del_old_runs.py
        env:
          GITHUB_TOKEN: ${{ secrets.AUTOTICK_BOT_TOKEN }}
